

function getAvrage(employees) {
    employees.forEach(employee => {
        console.info(employee.salary);
        sum = sum + employee.salary;
    });
    return 1;
}
const average = getAvrage(employees);
console average = getAvrage(employees);
console.info(average);